{
  "extends": ["@schematics/angular"],
  "schematics": {

    "child": {
      "aliases": ["lc"],
      "factory": "./src/child-state",
      "schema": "./src/child-state/schema.json",
      "description": "Adds a child state"
    },

    "state": {
      "aliases": ["ls"],
      "factory": "./src/root-state",
      "schema": "./src/root-state/schema.json",
      "description": "Adds a root state"
    }
  }
}